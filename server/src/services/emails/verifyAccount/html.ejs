<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css">
    <!-- Font Family -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        body {
            background-color: #f1f1f1;
        }

        html {
            font-family: 'Roboto', sans-serif;
            box-sizing: border-box;
        }

        p {
            font-size: 16px;
        }

        .email-container {
            padding-bottom: 10rem;
        }

        .text {
            margin: 1rem 0;
        }

        .email-logo {
            width: 100%;
            padding: 1rem 0;
        }

        .logo {
            margin: 0 auto;
            width: 200px;
            height: 100px;
            /* background-image: url("/img/logo/landing-logo.png"); */
            background-image: url("https://themes.pixelstrap.com/chitchat/assets/images/logo/landing-logo.png");
            background-position: center center;
            background-size: contain;
            background-repeat: no-repeat;
        }

        .email-body {
            margin: 0 auto;
            border-radius: 0.2rem;
            border: 2px solid rgba(175, 168, 168, 0.473);
            max-width: 40rem;
            /* height: 20rem; */
            background-color: #fff;
            padding: 4rem;
        }

        .btn-verify {
            display: inline-block;
            padding: 18px 20px;
            background-color: #0074d9;
            border-radius: 3px;
            text-decoration: none;
            color: #fff;
            font-weight: 600;
            font-size: 16px;

        }

        .email-btn {
            width: 100%;
            margin-bottom: 3rem;
            text-align: center;
        }

        .text-email {
            color: #0074d9;
            text-decoration: underline;
            font-weight: 600;
            font-size: 20px;

        }

        .note {
            font-size: 12px;
            color: #706f6f;
        }

        .username {
            color: #000;
            font-weight: 700;
            font-size: 16px;
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="email-logo">
            <div class="logo"></div>
        </div>
        <div class="email-body">
            <p class="text">Hello,</p>
            <p style="margin: 1rem 0 3rem 0;">Welcome <span class="username"><%= username %></span>! To complete your
                sign up, we just need to verify
                your email
                address: <span class="text-email"><%= receiverEmail %></span></p>
            <div class="email-btn"><a href="<%= linkVerifyAccount%>" class="btn-verify" target="_blank">Verify email
                    address</a></div>
            <p class="text">Alternatively, open the following URL in your browser:</p>
            <p class="text"> <a href="<%= linkVerifyAccount%>" target="_blank"><%= linkVerifyAccount%></a></p>
            <p style="font-size: 14px;" class="text">Enjoy it!</p>
            <p class="note">You’re receiving this email because you recently created a new <%= productName %> account or
                added a new
                email
                address. If this wasn’t you, please ignore this email.</p>
        </div>
    </div>
</body>

</html>